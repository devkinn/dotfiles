# --- Git ---
abbr -a g git

abbr -a gl git log
abbr -a gll git log --oneline

abbr -a gaa git add .
abbr -a gcm git commit -m

abbr -a gpl git pull
abbr -a gps git push

# --- kubectl ---
abbr -a k kubectl

# --- lazygit ---
abbr -a lg lazygit

# --- tmux ---
abbr -a tk tmux kill-ses
